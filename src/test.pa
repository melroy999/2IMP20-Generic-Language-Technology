process BOB { 
	initial state A := a.A + b.1 + c.0;
	state B := b.B;
	state C(n) := a.C(n) in range(0, 5);
	state C(n) := a.C(n) in range(6, 10);
	state D(n) := a.C(n) in range(0, 10);
}

process BOB2 {
	initial state A := a.a.a.a.A;
	state C(0) := a.B(0);
	state C(n) := a.B(n-1) in natural;
	state B(0) := a.C(0);
	state B(n) := a.C(n-1) in natural;
}

process BOB3 {
	initial state A := A;
}