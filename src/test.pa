process BOB { 
	initial state A := a.A + b.1 + c.0;
	state B := b.B;
}

process BOB2 {
	initial state A := a.A;
	state B(0) := a.A(1);
	state B(n) := a.A(n) in natural;
	state C(n) := a.C(n-1) + b.C(n+1) in range(1,5);
 	state C(n) := b.C(n-1) + a.C(n+1) in range(6,10);
}