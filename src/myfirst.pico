begin
	declare
		n : natural, s : string, b : boolean;
		
		
		n := 10;
		s := "a";
		while n do s := s || "a"; n := n - 1 od;
		for n := 10; n != 15; n := n + 1 do s := s || "a" od;
		
		b := not (not true or not false);
		b := b and n == 15
end